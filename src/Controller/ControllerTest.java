package Controller;

import Domain.Expressions.ConstExpr;
import Domain.Expressions.Expr;
import Domain.Stmt.AssignStmt;
import Domain.Stmt.IStmt;
import Repository.Repository;
import junit.framework.TestCase;

/**
 * Created by Patri on 11/2/2015.
 */
public class ControllerTest extends TestCase {
/*
    public void testChangeDebug() throws Exception {
        Repository repo = new Repository();
        Controller ctrl = new Controller(repo);
        assertEquals(false, ctrl.isDebugFlag());
        ctrl.changeDebug();
        assertEquals(true, ctrl.isDebugFlag());
        ctrl.changeDebug();
        assertEquals(false, ctrl.isDebugFlag());

    }

//    public void testOneStep() throws Exception {
//        Repository repo = new Repository();
//        Controller ctrl = new Controller(repo);
//        Expr exp = new ConstExpr(1);
//        IStmt stmt = new AssignStmt("a", exp);
//
//
//
//    }


    public void testIsDebugFlag() throws Exception {
        Repository repo = new Repository();
        Controller ctrl = new Controller(repo);
        assertEquals(false, ctrl.isDebugFlag());

    }
    */
}